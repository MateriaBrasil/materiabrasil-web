import React from 'react'

import Loading from '../Loading'

export default () => (
  <Loading
    style={{
      display: 'inline',
      padding: 14,
      position: 'relative',
      bottom: -6,
    }}
    color="inherit"
    size={20}
  />
)
